{
  "name": "studioflow",
  "private": true,
  "scripts": {
    "dev": "cd apps/desktop && pnpm tauri dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
