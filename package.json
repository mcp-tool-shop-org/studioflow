{
  "name": "studioflow",
  "private": true,
  "scripts": {
    "dev": "cd apps/desktop && pnpm tauri dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.79",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
